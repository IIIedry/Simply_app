<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.Weather" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="fontFamily">@font/poppins</item>
    </style>

    <style name="Theme.Weather" parent="Base.Theme.Weather"/>


    <style name="Theme.Weather_forecast" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_launcher_background</item>
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Weather_forecast</item>
    </style>
</resources>